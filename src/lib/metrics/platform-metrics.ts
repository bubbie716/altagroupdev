export type { PlatformMetrics } from "@/server/platform-metrics.service";

export const LIVE_PLATFORM_SOURCE = "Live platform data";
export const SIMULATED_MARKET_SOURCE = "Simulated market data";
export const PREVIEW_SOURCE = "Preview — not live operations";

/** Server entry point: `fetchPlatformMetrics()` in `platform-metrics.functions.ts`. */
