/** Alta Card fee configuration — isolated for future policy changes. */

/** Default late payment fee (ƒ). Charged once per overdue statement. */
export const ALTA_CARD_LATE_FEE_AMOUNT = 100;

/** Cash advance fee (ƒ). V1 default is zero — hook for future pricing. */
export const ALTA_CARD_CASH_ADVANCE_FEE_AMOUNT = 0;
